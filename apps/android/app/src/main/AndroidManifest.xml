<?xml version="1.0" encoding="utf-8"?>
<!-- Greenfield5 Android shell. No permissions are declared on purpose: the
     skeleton does no I/O. Capture/foreground-service/network permissions are
     progressive and feature-triggered (PRODUCT.md §8) and arrive with the
     features that need them. Note for Local mode: targetSdk 37 will require
     ACCESS_LOCAL_NETWORK handling (ADR-0006 follow-up 6). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Greenfield5">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
